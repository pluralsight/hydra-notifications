package hydra.notifications.client

// $COVERAGE-OFF
object NotificationParams {
  /**
    * Used to publish to a Slack channel other than the default channel.
    */
  val SlackNotificationChannelParam = "slack-notification-channel"
}
